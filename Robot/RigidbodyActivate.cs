using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*public class RigidbodyActivate : MonoBehaviour {

    public Component[] rigidbodys;


	// Use this for initialization
	void Start () {

        rigidbodys = GetComponentsInChildren<Rigidbody>();

    }
	
	// Update is called once per frame
	void Update () {

        if (Input.GetKey("f")) {

            EnableChildComponents();
        }
        if (Input.GetKey("g")) {

            DisableChildComponents();
        }
    }

    void EnableChildComponents() {

        foreach (Rigidbody col in rigidbodys) {
            col.enabled = true;
        }
    }
    void DisableChildComponents()
    {

        foreach (Rigidbody col in rigidbodys)
        {
            col.enabled = false;
        }
    }
}*/
